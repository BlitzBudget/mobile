import 'package:flutter/material.dart';
import 'package:mobile_blitzbudget/Screens/Authentication/Login/components/body.dart';

class LoginScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
